events {}
http {
  upstream opsknight { zone opsknight 64k; server web:3000 resolve; }
  resolver 127.0.0.11 valid=2s;
  server {
    listen 3000;
    location / { proxy_pass http://opsknight; proxy_connect_timeout 2s; proxy_read_timeout 15s; }
  }
}
