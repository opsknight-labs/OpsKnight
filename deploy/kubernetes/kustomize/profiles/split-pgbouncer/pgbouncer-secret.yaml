apiVersion: v1
kind: Secret
metadata: { name: opsknight-pgbouncer-auth, namespace: opsknight }
type: Opaque
stringData:
  # Replace this placeholder or use an external Secret before production.
  userlist.txt: |
    "opsknight" "opsknight_secure_password_change_me"
