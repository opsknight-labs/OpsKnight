{{- if .Values.pgbouncer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "opsknight.pgbouncer.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "opsknight.name" . }}-pgbouncer
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: {{ .Values.pgbouncer.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "opsknight.name" . }}-pgbouncer
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "opsknight.name" . }}-pgbouncer
        app.kubernetes.io/instance: {{ .Release.Name }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/pgbouncer-configmap.yaml") . | sha256sum }}
        checksum/auth: {{ include (print $.Template.BasePath "/pgbouncer-secret.yaml") . | sha256sum }}
    spec:
      automountServiceAccountToken: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: pgbouncer
          image: "{{ .Values.pgbouncer.image.repository }}:{{ .Values.pgbouncer.image.tag }}"
          imagePullPolicy: {{ .Values.pgbouncer.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.pgbouncer.securityContext | nindent 12 }}
          ports:
            - name: pgbouncer
              containerPort: {{ .Values.pgbouncer.port }}
          volumeMounts:
            - name: config
              mountPath: /etc/pgbouncer/pgbouncer.ini
              subPath: pgbouncer.ini
              readOnly: true
            - name: auth
              mountPath: /etc/pgbouncer-auth
              readOnly: true
            - name: run
              mountPath: /var/run/pgbouncer
            - name: tmp
              mountPath: /tmp
          readinessProbe:
            tcpSocket:
              port: pgbouncer
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: pgbouncer
            periodSeconds: 10
            failureThreshold: 3
          resources:
            {{- toYaml .Values.pgbouncer.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "opsknight.pgbouncer.fullname" . }}
        - name: auth
          secret:
            secretName: {{ include "opsknight.pgbouncer.fullname" . }}-auth
        - name: run
          emptyDir: {}
        - name: tmp
          emptyDir: {}
{{- end }}
