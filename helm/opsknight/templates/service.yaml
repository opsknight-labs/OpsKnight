apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsknight.fullname" . }}
  labels:
    {{- include "opsknight.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsknight.selectorLabels" . | nindent 4 }}
