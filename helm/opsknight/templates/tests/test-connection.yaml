apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsknight.fullname" . }}-test-connection"
  labels:
    {{- include "opsknight.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsknight.fullname" . }}:{{ .Values.service.port }}/api/health']
  restartPolicy: Never
